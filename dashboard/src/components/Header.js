export default function Header() {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "space-between",
        marginBottom: 20,
      }}
    >
      <h1>📊 Dashboard</h1>
      <button>🌙</button>
    </div>
  );
}
